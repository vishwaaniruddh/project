TRUNCATE `audit_logs`;
TRUNCATE `daily_material_usage`;
TRUNCATE `daily_work_photos`;
TRUNCATE `installation_delegations`;
TRUNCATE `installation_materials`;
TRUNCATE `installation_notifications`;
TRUNCATE `installation_progress`;
TRUNCATE `inventory`;
TRUNCATE `inventory_dispatches`;
TRUNCATE `inventory_dispatch_items`;
TRUNCATE `inventory_inwards`;
TRUNCATE `inventory_inward_items`;
TRUNCATE `inventory_movements`;
TRUNCATE `inventory_reconciliation`;
TRUNCATE `inventory_reconciliation_items`;
TRUNCATE `inventory_stock`;
TRUNCATE `inventory_tracking`;
TRUNCATE `material_dispatches`;
TRUNCATE `material_requests`;
TRUNCATE `sites`;
TRUNCATE `site_delegations`;
TRUNCATE `site_surveys`;